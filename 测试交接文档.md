# 雷达易伤测试 · 交接文档（2026-09-03）

## 2026-09-05 非核心代码整理

测试与离线代码统一入口见 `scripts/README.md`。主报告命令改为 `python3 scripts/gen_report_combined.py g1 g2 g3`；旧易伤评估、硬编码报告恢复和一次性finalize脚本已删除，下方历史章节提到这些文件时不再代表可用入口。离线模型构建脚本迁至 `scripts/offline/build_position_prior.py`，模型及数据路径不变。核心源码、生产配置、报告和bag保持原样。生成的固定R源码副本保留在log中作为带哈希的构建证据；维护源在scripts内。

## 2026-09-05 图表显示调整

按用户要求，六份报告的单机易伤合计柱状图统一收紧为0–1500秒、每300秒一格，保留零起点。统计数据和其他图表不变；生成器同步更新。本次显示调整后的主报告文件哈希发生变化，先前“主报告未变”结论仅指对照重测阶段。调整前副本及数据不变性核验见 `log/rerun68/chart_axis_20260905/`。

## 2026-09-05 更新：三组功能对照已完成重测并替换报告

> 本节优先于下方历史状态，尤其是“三份配置对比未重测”的描述。9月4日三份主报告保持原样；本次三份对照报告使用各自新录制的基线。

完成10次有效完整回放，每配置2次，第二组反转运行顺序；g3两份报告共用同一对生产基线。固定时间映射、按比赛节奏重放、5Hz厘米量化、仅用已接收且未过期的猜点。死亡后实际输出继续判定，不按DB血量删除；存活时长另列。对照库和参数均隔离在log目录，625个生产文件哈希核验无变化。

下表为“生产功能开启 − 对照”的逐车合计时长差，两次均值。A是纯跟踪，B是跟踪＋猜点；不是实际伤害增益。

| 对照报告 | A差值(s) | B差值(s) | 共同时间偏移±2s下B均值差范围(s) | 结论 |
|---|---:|---:|---:|---|
| `log/report_68g1_armor_dyn/report.html` | −50.8 | +62.9 | +42.6～+102.8 | 动态选点在本局B上重复正收益，A下降 |
| `log/report_68g3_cov_cmp/report.html` | −18.8 | +120.2 | +90.5～+125.4 | 射线协方差在本局B上重复正收益，A下降 |
| `log/report_68g3_navgrid_cmp/report.html` | −3.0 | +3.6 | −22.9～+12.5 | 时间偏移会反转方向，未确认稳定正收益 |

每份报告含并集/合计柱状图（两次均值及最小/最大范围，非置信区间）、逐次/逐车数据、时间线和敏感性分析。armor对照仍允许安全car回退；固定R影响选择器和跟踪，不能仅归因于Kalman；NavGrid与盲区一起关闭、驻留锚点保持0.8，未单独分离两项贡献。

一次fixed录制因助手检查工具读取正在写入的SQLite bag引发锁冲突中断，已保留、排除，并以相同配置完整重跑；10次有效结果不含该失败尝试。以后只查询已停止录制并完成导出的bag。

核验已完成：实际运行参数、测量R、来源/接收时序、逐事件积分、图表数值/结构/链接，以及生产文件和主报告不变性。未进行浏览器视觉验收。原始bag、参数和账本均保留：

- `log/rerun68/cmp05_{variant}_r{repeat}_{g1或g3}/`：10次有效运行及各自分析。
- `log/rerun68/comparison_20260905/comparison_audit.json`：完整核验（status=complete）。
- `log/rerun68/comparison_20260905/results.json`：报告哈希及差值汇总。
- `log/rerun68/comparison_20260905/previous_reports/`：替换前对照报告。
- `log/rerun68/comparison_20260905/excluded_runs.json`：失败排除记录。
- `scripts/rerun68/COMPARISON.md`：具体对照定义、工具及复生成命令。

只能称为当前录像、当前规则假设下的对照验证。两次重复不能证明统计显著或跨比赛泛化；DB缺少干扰波压制和定位离线区间，仍假设定位在线且无压制，本地接收不等于裁判收包。不能宣称已完整复现比赛实际易伤效果。

## 2026-09-04 更新：三局已按修正口径重测

> 本节优先于下方2026-09-03历史记录。旧报告数据和“bag已全部删除”等描述只适用于旧轮次；本轮原始数据完整保留。

- 核心源码、生产配置、模型、场地资源及已加载组件哈希核验未变；测试专用夹具加载原有组件，Qt/debug image关闭。
- 修复评估：复活不截断0.5秒中断计时；P/x用整数十分之一分；按预测条目slot_idx查找；仅使用已接收且未过期的猜点；不再用未来消息或DB死亡状态过滤输出。
- 06/07/08.mp4 分别核验为68g1/g2/g3。在预录上冻结“视频内容秒→比赛秒”映射后，按比赛节奏重放；基于bag本地接收时间评估，包含原帧至本地输出的处理延迟。
- 报告提供±2秒时间偏移、额外100/200ms通信延迟和4/5/10Hz假设发送器的敏感性结果。当前没有裁判服务器收包/射频压制/定位离线实测，不称为实战效果证明。
- 主表与P时间线按通信协议0x0305的5Hz上限计算，并进行厘米量化；未限频及10Hz仅作超协议上限的诊断。另含130ms/1%与200ms/3%丢包网络情景，不声称是链路实测。
- 3份主报告已替换；3份历史配置对比只标注旧口径，未混入条件不同的新数据。所有6份旧原文保存在 `log/rerun68/previous_reports/`。

| 局 | GID | 5Hz A并集(s) | 5Hz B并集(s) | 5Hz A合计(s) | 5Hz B合计(s) | 地图接收Hz | 延迟中位/P95(ms) |
|---|---|---:|---:|---:|---:|---:|---:|
| 68g1 | 1778913533396 | 373.2 | 417.4 | 658.7 | 1353.9 | 12.82 | 83.1/91.2 |
| 68g2 | 1778914365365 | 360.8 | 417.2 | 694.5 | 1320.8 | 11.81 | 80.6/90.9 |
| 68g3 | 1778915208150 | 368.3 | 417.4 | 641.8 | 1317.8 | 12.33 | 103.4/165.8 |

并集/合计均为条件模拟的P≥100标记阈值时长，存活期间的时长另见报告。双倍只统计条件机会解锁时刻，不代表已经实际触发。g2仍属于当前先验模型训练集。

可复核产物：

- `log/rerun68/realtime_g1|g2|g3/`：完整bag、source_frames.csv、events.jsonl、summary.json、per_robot.json、ledger_A/B.jsonl、export_audit.json。
- `log/rerun68/alignment.json`：冻结的内容时间映射及独立内容块残差。
- `log/rerun68/core_before.json`、`runtime_manifest.json`、`completion_audit.json`：核心不变性、运行组件和完成核验。
- `scripts/rerun68/README.md`：完整方法、限制与复跑命令。
- `python3 scripts/gen_report_combined.py g1 g2 g3` 已转到新评估器，可直接重生成本轮报告，勿再调用旧merged-CSV评估流程。

浏览器本地file URL预览被安全策略拒绝；已完成HTML表格、图表结构和链接的文件级核验，未声称浏览器视觉验收。

---


> 本文件供新接手的智能体快速了解：仓库当前状态、完整测试工作流、已完成的报告、关键实验结论与坑。
> 仓库：/home/delphine/rm/tensorrt10_detect（ROS2 Jazzy / TensorRT10 雷达站，RMUC2026）

---

## 0. 仓库当前状态（交接时刻）

- **代码 = 原始版本**：测试调参相关代码已全部移除并固化到 git（HEAD = ae301ae，由 fd26301/ae301ae 两次提交完成回滚）。源码中**无** test_config / NIS / 协方差模式开关 / prior 开关等任何测试逻辑。
- **配置（生产）**：
  - `configs/tracker.yaml`：kalman_gate_box=-1（box NIS 关）、kalman_gate_world=40（world NIS 松阈值，提供"越稳定越严格"自适应物理门）
  - `src/tensorrt_detect/config/ros2_params.yaml`：projection_pixel_sigma_px=4.0（ray 协方差），无测试板块
- **log/ 仅保留**：`gen_report_combined.py`（主报告生成器）+ 6 份报告 HTML + COLCON_IGNORE。**全部 bag 已删除**，报告为静态终稿不可再生成（g1/g2/g3 可重录后由 gen_report_combined.py 复用）。
- **未提交/杂项**：pose_node.cpp 只有新增中文注释（无功能变化）；`archify/` 为未跟踪的架构可视化产物（rm26radar_runtime.*），可删可留。
- ⚠ **构建**：9-03 21:xx 有 colcon 构建日志，应已用当前源码重建；不确定时 `colcon build --packages-select tensorrt_detect position_prior` 后重启节点。

---

## 1. 测试在测什么（口径）

用**裁判系统真实数据（DB）** 当标准答案，把雷达录制输出按 **RMUC2026 雷达规则**模拟"易伤标记进度 P"：

| 方案 | 输入 | 说明 |
|---|---|---|
| 真实比赛(DB) | sqlite timeseries 是否易伤列 | 裁判实际产生的易伤 |
| A 纯跟踪 | /radar_map 检测坐标 | 只用检测 |
| B 跟踪+猜点 | radar_map + /prior_predictions 主猜点（**只用 top-1**，candidates[] 未参与） | 检测缺失时补猜点 |

规则：d<0.8m 准确(+1.0) / <1.6m 半准确(+0.5) / ≥1.6m 错误(−0.8)；每 0.5s 无数据=中断(−0.8)；复活 x 清零 P 保留；P∈[100,120)=15% 易伤、≥120=20%；双倍易伤=并集每 60s 1 次上限 2。
**死亡车辆猜点过滤**：DB 血量≤0（±1.5s）的猜点丢弃并按中断处理（报告第 5 节有死亡/总数）。

**关键指标注意**：
- **并集易伤饱和**（416/420≈99%）：对配置差异极不敏感；要看**判定分类（错误数/有效占比）、单机合计、B−A 相对退化**
- **重放方差极大**：A 层（配置无关）合计波动常达 ±10~27s、有效占比 ±5~8pp → 单次对比结论都需重复验证
- **A 层是"方差标尺"**：B 的差异 = 重放方差 + 配置差异；"B−A 相对退化"是更干净的指标

---

## 2. 录制工作流（你的机器）

1. 前置：`colcon build` + `source install/setup.bash`
2. 配置：`src/tensorrt_detect/config/ros2_params.yaml`（video_path/fps=20）；改 config 后**必须重启节点**（参数只在启动时读取）
3. 启动主链：launch（`ros2 launch src/tensorrt_detect/launch/detect_pipeline.launch.py mode:=video enable_qt_display:=true`）或独立脚本 `scripts/run_noqt_test.sh`
4. 录制：`./scripts/record_referee_topic.sh`（5 话题：armor_detections/world_targets/radar_map/prior_predictions/pipeline_timing；不含图像）→ 产出 `log/bags/referee_YYYYmmdd_HHMMSS/`
5. 同一视频重放多次 → 时间轴映射每次不同 → 每 bag 需独立拟合

---

## 3. 数据处理工作流（脚本，本环境可直接跑）

| 步骤 | 脚本 | 说明 |
|---|---|---|
| 1 导出 | `scripts/export_bag_to_csv.sh <bag>` | ros2 bag play --rate 50 + topic echo --csv；csv_cleanup.py 去日志/生成表头（RadarMap 27 列 / PriorPrediction 535 列） |
| 2 融合 | `scripts/merge_topics_csv.py <bag>/csv` | radar_map 每帧时间戳最近邻(≤50ms)匹配 prior → merged_video.csv（匹配率~99%） |
| 3 时间轴拟合 | fit_gid.py（临时） | match_t=(wall_t−b)/a；numpy 网格+细化；只算 DB 存活机器人；输出 a/b/RMS |
| 4 墙钟频率 | 读 pipeline_timing fps 列中位 | 只报墙钟实测，不换算比赛频率 |
| 5 报告 | `log/gen_report_combined.py [g1|g2|g3]` | 改 GAMES 配置（gid/bag/a/b/wall_hz/rms）后生成 |

**拟合脚本（fit_gid.py）不在仓库**（log/ 清理时删了）——需要时按"网格 a∈[0.6,1.2]×b∈[-40,100] + 细化"重建，逻辑简单。

---

## 4. 已完成的报告（6 份，静态终稿）

| 报告 | 内容 | bag | 关键结果 |
|---|---|---|---|
| report_68g1 | 68局1 动态录制 | 200239 | DB 318.0/581.0；A 401.1；**B 412.3s (+30%)**，双倍 2/2 |
| report_68g2 | 68局2 动态录制 | 003045 | DB 321.0/600.0；A 393.8；**B 416.6s (+30%)** |
| report_68g3 | 68局3 动态录制 | 003748 | DB 289.0/490.0；A 386.3；**B 416.5s (+44%)** |
| report_68g1_armor_dyn | 纯armor vs 动态（射线求交模式） | 195514 / 200239 | 动态更优：B 并集 412.3 vs 399.7（+12.6s）；RMS 1.17 vs 1.31 |
| report_68g3_navgrid_cmp | navgrid/盲区 全开 vs 全关 | 213046 / 212314 | 几乎无差（B 并集 417.5 vs 417.5，差<2s 方差内）→ **保留 navgrid/盲区** |
| report_68g3_cov_cmp | 测量协方差 fixed(σ1.0) vs ray | 232054 / 213046 | **ray 更优**：B 417.5 vs 411.7；有效 71.3% vs 60.9%（恢复版，缺 P 时间线图） |

DB GID：68局1=1778913533396（红胜）、68局2=1778914365365（蓝胜）、68局3=1778915208150（蓝胜），均为华科(红) vs 中山学院(蓝)。

---

## 5. 实验结论与坑（重要，避免重蹈）

1. **测试板块已整体移除**（结论：未带来可测收益）。三坑教训：
   - rclcpp 参数文件**按顶层节点名过滤**——顶层键必须是 `/**`、键名带点号前缀，否则整段不生效
   - `scripts/run_noqt_test.sh` 启动 position_prior 必须同时传 `ros2_params.yaml`（曾只传 prior yaml 导致开关全失效）
   - 参数只在节点启动时读取 → 改配置必须重启节点
2. **NIS 实验放弃**：标准卡方(18.467/13.816)对当前 Kalman 模型过严（box R 偏小 ~1000x）→ 检测崩到 3031；标定 R(box10px/σ9)+谷底阈值后可用，但纯净 NIS on/off 对比无一致收益（±2pp 方差内），且 R 标定疑似伤猜点 → **全部回滚**。原始 world=40 一直提供"越稳定越严格"的自适应物理门（不需要额外代码）。
3. **navgrid/盲区先验/stay：保留**（全开 vs 全关无差；防穿墙/热区拉偏的保险价值 > 成本）。
4. **fixed 协方差模式：不用**（ray 的方向相关协方差完胜）。
5. **猜点只用 top-1（prior_world_x/z）**；话题里发 top-5 candidates 未参与模拟——若想做"信息上限"分析可用 candidates[]（未做过）。
6. csv_cleanup.py 的 PriorPrediction 列顺序曾与 msg 不符（fallback_level/sample_count 位置），已修正——**改动前先对 msg 定义核对**。
7. 任何比较类结论都要注意**单次录制重放方差**；B−A 相对指标 + 重复录制才是可靠路径。

---

## 6. 交接待办/建议

- 若要出新的比赛报告：重录 → 导出/融合/拟合 → 改 `gen_report_combined.py` 的 GAMES → 生成
- report_68g1_armor_dyn / navgrid_cmp / cov_cmp 的生成脚本已删除；要复现需重建（cov_cmp 已有恢复版脚本 gen_report_cov_cmp_restore.py 在 log/）
- `archify/` 里有 rm26radar_runtime 的架构/时序图（HTML/PNG），可作可视化参考
