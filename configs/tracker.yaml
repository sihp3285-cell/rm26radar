# Tracker 参数配置
# 固定槽位多目标跟踪器参数
max_miss: 6               # 连续丢失多少帧后槽位标记为 DEAD（不再输出）
max_predict: 3           # 连续丢失多少帧内保持 PREDICTED（卡尔曼外推仍显示在地图上）
min_hit: 2                # 最少命中次数才对外输出
max_gate_box: 1500.0       # 像素框中心距离门限（匈牙利匹配代价矩阵门限）
